[package]
name = "registrar"
version = "0.1.0"
edition = "2021"

[dependencies]
soroban-sdk = "23.1.0"

[dev-dependencies]
soroban-sdk = { version = "23.1.4", features = ["testutils"] }

[lib]
crate-type = ["cdylib", "rlib"]
